# DI (Dependecy Injection)

DI frameworks exists for GO, but is not the default and not the prefered way to do DI in GO.

As explained in [this](https://rednafi.com/go/di-frameworks-bleh/) article, using a framework can introduce a touch of _magic_ in the code, since the declaration is separated from the real use in code.
